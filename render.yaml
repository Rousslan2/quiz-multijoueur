# FoodPlug — serveur statique + API commandes.

services:
  - type: web
    name: foodplug
    runtime: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "22.22.0"
